// Deprecated: moved to Level.allowedDeletePaths in `src/constants.tsx`.
// This file is kept temporarily for backwards compatibility but should
// not be used by new code. The allowlist now lives on Level definitions.
export const LEVEL_DELETE_ALLOWLIST: Record<number, string[][]> = {};

export default LEVEL_DELETE_ALLOWLIST;
